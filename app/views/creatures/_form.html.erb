<div class ='container'><%= bootstrap_form_for(@creature) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.submit %>

<% end %>

</div>